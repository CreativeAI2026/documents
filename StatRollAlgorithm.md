# ステータス算出アルゴリズム

装備品の**ステータスをどう決めるか**の数理モデル。**調合（素材2つ → 1つ）**と**ドロップ（拾得）**の両方がこのモデルを通る（違いは親が居るかどうかだけ → 「[ドロップ（拾得）のロール](#ドロップ拾得のロール)」）。

> **適用範囲：このモデルは装備品のみ**。食材はこの確率ロールを使わず**全ステータス固定（決定的）**で扱う。カテゴリで扱いが分かれる：
>
> | カテゴリ | ステータスの決め方 | 個体差 |
> |---|---|---|
> | **装備品** | 本モデルで**端末でロール**（合成前も合成後も1つ1つ違う） | あり |
> | **食材** | **固定ルール** | なし |

## このモデルの正体

> **減衰パワー予算の上に乗る、シナジー重み付きディリクレ配分モデル**

ひとことで言うと「**総パワー（決定的）× 配分（確率的）**」の合成モデル：

$$
r_s = B \cdot p_s
$$

- $B$ … 総パワー（どれだけ強いか）。親から決定的に算出、成長と上限つき。
- $p = (p_s)$ … 配分割合（どのステータスに寄るか）。$\sum_s p_s = 1$ を満たし、**ディリクレ分布**からロール。

連鎖合成は「上限つきのマルコフ過程」になる＝**何段重ねても発散しない**。

---

## 記法

- 素材A・Bのステータスベクトル： $a = (a_s)$, $b = (b_s)$（各 $\ge 0$、$s$ はステータス型）
- 対象ステータス： $S = \{\, s : a_s > 0 \ \lor\ b_s > 0 \,\}$（AまたはBが持つ型の和集合）
- 結果： $r = (r_s)$
- ステータス型は Specification.md「アイテムカテゴリと付与ステータス」で定義：攻撃% / 防御% / 会心ダメージ / 会心率 / 最大HP%。**1アイテムの付与数は最大2つ。** 本モデルの入出力は**装備品**（装備品同士の調合）のみを対象とする（食材は固定値・上記「食材の固定ルール」、武器は調合不可）。

---

## アルゴリズム

### 層1：パワー予算 $B$（総量・成長・上限）

$$
\mathrm{power}_A = \sum_{s} a_s,
\qquad
\mathrm{power}_B = \sum_{s} b_s
$$

「強い方を土台に、弱い方をボーナスで足す」ので**合成すると必ず強い方の親以上**になる（劣化しない）：

$$
\mathrm{base} = \max(\mathrm{power}_A, \mathrm{power}_B), \qquad
\mathrm{sub}  = \min(\mathrm{power}_A, \mathrm{power}_B)
$$

$$
B = \mathrm{base} + (C_{\mathrm{cap}} - \mathrm{base})\left(1 - e^{-\beta\,\mathrm{sub} / (C_{\mathrm{cap}} - \mathrm{base})}\right)
$$

- 弱い方 $\mathrm{sub}$ で「残り容量 $C_{\mathrm{cap}} - \mathrm{base}$」の一部を埋める。$\beta$ がその埋め具合（成長量）。
- $B \ge \mathrm{base}$＝**非劣化を保証**、かつ $B < C_{\mathrm{cap}}$＝**上限に漸近**（天井に張り付かない・連鎖インフレ無し）。
- 簡易版：$B = \min\!\bigl(C_{\mathrm{cap}},\ \mathrm{base} + \beta\,\mathrm{sub}\bigr)$（ハードキャップ）でも非劣化は保証。実装が楽。同じ入出力なので後で差し替え可。

### 層2：配分 $p$（分布・尖り・シナジー）

両親が持つ型をシナジーで増幅した基礎ウェイト：

$$
w_s = (a_s + b_s)\,\bigl(1 + \sigma \cdot \mathbb{1}[\,a_s > 0 \,\land\, b_s > 0\,]\bigr)
$$

**付与数は最大2つ**（Specification「アイテムカテゴリと付与ステータス」）なので、ウェイト上位2型 $\{s_1, s_2\}$ に絞ってから正規化して集中度ベクトルへ：

$$
\hat{w}_s = \frac{w_s}{w_{s_1} + w_{s_2}}\ \ (s \in \{s_1, s_2\}),
\qquad
\alpha_s = \alpha_0 \, \hat{w}_s
$$

配分をディリクレ分布からロール：

$$
p \sim \mathrm{Dirichlet}(\alpha),
\qquad p_s \ge 0,\quad \sum_s p_s = 1
$$

- $\sigma$ … シナジー強度。$\mathbb{1}[\cdot]$ は条件成立で 1、それ以外 0（指示関数）。
- $\alpha_0$ … 集中度。$\mathbb{E}[p] = \hat{w}$ で、$\alpha_0$ が散らばりを決める：
  - $\alpha_0$ **小 → 尖る**（1ステータスに偏った当たり/はずれ）
  - $\alpha_0$ **大 → 均等**（$\hat{w}$ 通りに平たく）

### 合成と整理

$$
r_s = B \cdot p_s \qquad (s \in \{s_1, s_2\})
$$

結果は上位2型に $B$ を配分したもの。$r_s < \varepsilon$ なら実質1型扱い。会心率など上限のある型は個別にクランプ（例：会心率 $\le 100\%$）。

---

## ドロップ（拾得）のロール

フィールドに落ちている装備品（合成前のシード）にも同じロールモデルを使う。違いは **親が居ない** ことだけ。

| | 調合（素材2つ） | ドロップ（拾得） |
|---|---|---|
| 総パワー $B$ | $\mathrm{base} + $ ボーナス（層1） | **アセットの固定ステータス合計**（$C_{\mathrm{cap}}$ でクランプ） |
| 対象の型 | 両親が持つ型のウェイト上位2つ | **重み付き非復元抽出**（Specification.md §2.1.1 の表・最大2つ） |
| 配分の期待値 $\hat{w}$ | シナジー重み付き | **均等**（$\hat{w}_s = 1/n$。親が居ないので偏らせる根拠がない） |
| 配分のロール | $\mathrm{Dirichlet}(\alpha_0\hat{w})$ | 同じ（$\alpha_s = \alpha_0/n$） |
| 整理 | $\varepsilon$ 切り捨て・型ごとのクランプ | 同じ |

$$
B = \min(C_{\mathrm{cap}},\ \textstyle\sum_s \mathrm{seed}_s),
\qquad
\alpha_s = \frac{\alpha_0}{n},
\qquad
p \sim \mathrm{Dirichlet}(\alpha),
\qquad
r_s = B \cdot p_s
$$

- アセットの固定値は **総量の宣言**として読む（どの型に書いてあるかは無視する）。型は抽選で決まるため。
- $\alpha_0$ は調合と共用なので、ドロップも「尖った当たり/はずれ」が同じ強さで出る。
- 食材はドロップでもこのモデルを通さない（HP即時回復の固定ルール）。

---

## パラメータ（調整ノブ）

| 記号 | 意味 | 効果 |
|---|---|---|
| $\beta$ | ボーナス係数 | 弱い方の素材が上乗せする量（成長の大きさ）|
| $C_{\mathrm{cap}}$ | 総パワー上限 | 強さの天井（連鎖インフレを止める）|
| $\sigma$ | シナジー強度 | 共通ステータスの増幅＝組み合わせ戦略の効き |
| $\alpha_0$ | 集中度 | 個体差：小=尖る / 大=均等 |
| $\varepsilon$ | 切り捨て下限 | これ未満の型は捨てる（付与数の上限は Specification「アイテムカテゴリと付与ステータス」より最大2で固定）|

---