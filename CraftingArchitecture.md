# 調合システム アーキテクチャ

2つの素材を合成して新しいアイテムを作る。

- **ステータス** … 実行時に式で計算（AI不要）
- **名前・画像** … 開発時に作っておき、実行時はDBから引くだけ

---

## 全体像

```
【開発時：大学GPUサーバー】
  有効な全ペアを定義 → 画像・名前・説明をまとめて自動生成
        → SQLite + 画像 にまとめてゲームに同梱

【実行時：ゲーム】
  調合(A,B) → ステータスをロール → recipeHash でDB検索 → 結果を表示
```

実行時に生成はしない。有効なペアを全て用意済みなので**必ずヒット**する（待ち時間ゼロ）。

---

## 3つの柱

### 1. ステータス（実行時に計算）
素材2つのステータスだけから確率的にロールする。
- 同じレシピでも個体差が出る（当たり/はずれ）
- 一度ロールしたら固定。連鎖合成でも破綻しない（上限あり）
- 詳細 → [CraftingStatAlgorithm.md](./CraftingStatAlgorithm.md)

### 2. 名前・画像（事前に用意）
- 大学GPUサーバーで **画像も名前/説明もまとめて自動生成**（品質優先・時間制約なし）
- 自動生成のワークフロー・構築手順は別途設計（専用ドキュメントは現状なし）

### 3. カタログ（実行時に引く）
- キー＝素材ペア（順不同）＝ **RecipeHash**（A+B == B+A）
- **有効な全ペアを用意**（装備品同士・食材同士のみ、カテゴリ跨ぎ・武器は対象外 → GameSystems 3.3）＝ 許可された組み合わせなら必ず結果が出る
- 調合結果もカタログ内のアイテム → 連鎖しても増えない（閉じている）

---

## データの持ち方

```
catalog.db （SQLite・読み取り専用・同梱）
   items   : item_id  → { name, description, image_path, category }
   recipes : pair_key（=RecipeHash） → result_id
images/<item_id>.png （同梱）
```

- **items＝アイテム図鑑、recipes＝ペア→結果** の2テーブル。結果は必ず items 内を指す（連鎖しても増えない＝閉じている）
- ステータスはDBに入れない（実行時にロール）
- セーブデータ（所持品・ロール済みステータス）は別管理
- Unityからは sqlite-net 等でアクセス
