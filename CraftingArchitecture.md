# 調合システム アーキテクチャ

2つの素材を合成して新しいアイテムを作る。

- **ステータス** … 実行時に式で計算(端末・AI不要)
- **素材(合成前)** … 名前・画像・説明ともゲームに同梱(local)。オフラインでも図鑑・インベントリ表示できる
- **結果(合成後)** … 名前・説明・画像を **CDN に静的ファイル**(`results/<RecipeHash>.json` + 画像)で置き、調合時に取得(ローディングで隠す)→端末にキャッシュ

> なぜ結果をCDNに出すか: 調合は「調合場所」での離散アクションで **ローディング演出を挟む**ため、結果取得のレイテンシはその裏に隠せる。ならば結果カタログ(2500件超の名前・説明・画像)を丸ごとゲーム外へ出せば **同梱は素材だけ**で済む。ポイントは **サーバー/APIではなく静的ファイル**にすること — recipe解決もステータスも端末側なので、CDNは「結果の中身を返す静的JSON+画像」だけ。サーバー運用も課金も無い(**Cloudflare R2** の無料枠で完結)。
>
> 注意: ローディングは **遅延は隠せるが不通は隠せない**。**未キャッシュの新規調合はネット接続が必要**(=調合はオンライン寄りのアクション)。一度作って所持した結果はキャッシュからオフラインでも閲覧できる。レシピの秘匿はしない(静的ファイルは列挙可能)。

---

## 全体像

```
【開発時：大学GPUサーバー】
  有効な全ペアを定義 → 名前・説明・画像をまとめて自動生成
        → 結果ごとに results/<RecipeHash>.json + 画像 を R2(Cloudflare)に配置

【実行時：ゲーム(調合場所)】
  A,B を選択 →(ローカルでカテゴリ検証)→ ローディング表示
     → RecipeHash を算出 → CDN の results/<hash>.json + 画像 を取得
     → 素材の stats からステータスをロール(端末)
     → 結果を表示 → JSON/画像を端末にキャッシュ
```

実行時に生成はしない(結果は事前生成済み)。有効なペアは全て用意済みなので **必ず結果がある**。取得レイテンシはローディングで隠れる。

---

## 3つの柱

### 1. ステータス（端末で計算）
素材2つのステータスだけから確率的にロールする。**サーバーもCDNも使わず端末で完結**(オフライン可)。
- 同じレシピでも個体差が出る（当たり/はずれ）
- 一度ロールしたら固定（セーブに個体ごと保存）。連鎖合成でも破綻しない（上限あり）
- 詳細 → [CraftingStatAlgorithm.md](./CraftingStatAlgorithm.md)

### 2. 名前・説明・画像（事前生成 → CDNに静的配置）
- 大学GPUサーバーで **画像も名前/説明もまとめて自動生成**（品質優先・時間制約なし）
- 結果ごとに **`results/<RecipeHash>.json`(名前・説明・画像URL・カテゴリ)+ 画像** を CDN に静的配置。調合時に取得し、端末にキャッシュ
- 自動生成のワークフロー・構築手順は別途設計（専用ドキュメントは現状なし）

### 3. カタログ（CDN上の静的ファイル・サーバー無し）
- キー＝素材ペア（順不同）＝ **RecipeHash**（A+B == B+A）＝ **JSONのファイル名**。解決は「hash算出 → 該当ファイルを取得」
- **有効な全ペアを用意**（装備品同士・食材同士のみ、カテゴリ跨ぎ・武器は対象外 → GameSystems 3.3）。同カテゴリのペアには必ず結果ファイルがある。カテゴリ検証はローカルの素材情報でできるので、無効ペアは取得前に弾ける
- 調合結果もカタログ内のアイテム → 連鎖しても増えない（閉じている）。連鎖時は所持済み結果(キャッシュ)を素材として扱う

---

## データの持ち方

```
[local 同梱]
  素材(基本アイテム) : item_id → { name, description, image, category, stats }
  images/<素材 item_id>.png    # 素材画像だけ同梱
  ※ RecipeHash ロジック・ステータスロール算法はコード

[CDN(静的・サーバー無し)]
  results/<RecipeHash>.json : { result_id, name, description, image_url, category }
  images/<result_id>.(png/webp)   # 結果画像 2500枚以上

[client cache / save]
  取得済みの結果JSON・画像      # 一度取ったら端末に保存 → 以後オフライン可
  所持品・ロール済みステータス  # → SaveSystem.md
```

- 結果は必ずカタログ内のアイテムを指す（連鎖しても増えない＝閉じている）。連鎖時の素材となる所持済み結果は `result_id` で識別
- **ステータスはCDNに置かない**（端末でロールし、セーブに個体ごと保存）。CDNのJSONは「結果が何か(名前・説明・画像・カテゴリ)」だけ
- **同梱は素材だけ**。結果(テキスト+画像)は全て CDN に外出しして同梱容量を軽くする
- 取得レイテンシはローディングで隠す。**未キャッシュの新規調合はネット必要**、既取得はオフラインでも閲覧可
- UnityからローカルDBは sqlite-net 等、CDNからは HTTP で静的ファイルを取得
- 配信は **Cloudflare R2 +(本番は)カスタムドメイン**(エッジキャッシュが効く。公開用 `r2.dev` URL はレート制限のある開発用)。
