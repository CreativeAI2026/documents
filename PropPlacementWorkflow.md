# 小物配置の分担(Field_Area01 を複数人で同時に触る)

フィールドの**形**(壁・床・ガラス・柵・扉)は [`MapLayout.md`](./MapLayout.md) の図から生成する。
この文書は、その上に**小物を置く作業を複数人で並行して進める**ための段取りをまとめたもの。

---

マップの形は `documents/MapLayout.md` から生成する物なので、**`Field_Area01.unity` は誰も直接編集しない**。
小物(机・椅子・本棚・置きアイテム)は**担当ごとの別シーンに置いたまま**にし、**実行時に Additive で重ねる**。
同じ `.unity` を複数人が触らないので、**git の競合が構造的に起きない**(unityyamlmerge が無くても安全)。

**畳まない**のが要点。`Field_Area01` に置いた `PropScenes`(`AdditiveScenes` コンポーネント)が
小物3枚を実行時に読み込むので、**分けたままリリースまで通せる**。1枚に畳む必要は最後まで無い。

| ファイル | 中身 | 触る人 |
| -------- | ---- | ------ |
| `documents/MapLayout.md` | マップの形の定義(唯一の正) | **マップ形状担当だけ** |
| `Field_Area01.unity` | `Map`(生成物) / スポーン / ライト | **マップ形状担当が生成ツールで更新**。小物配置担当は編集しない |
| `Field_Area01_Props_1F.unity` | `Props_1F` 配下の小物 | 1F の小物配置担当 |
| `Field_Area01_Props_2F.unity` | `Props_2F` 配下の小物 | 2F の小物配置担当 |
| `Field_Area01_Props_3F.unity` | `Props_3F` 配下の小物 | 3F の小物配置担当 |

## 作業手順(各小物配置担当)

1. **`Tools > CreativeAI > Map > 小物シーン > 2F を開く`**(担当の階を選ぶ)
   - マップと担当シーンが**重ねて開く**(Additive)。画面上は1つの空間なので、床の上に普通に置ける
   - 担当シーンが**アクティブ**になる(Hierarchy で太字)。新しく置いた物はここに入る
   - `Map` は**ピッキング無効**になる。Scene ビューで壁や床を掴めないので、誤ドラッグで形が崩れない
2. 小物を置く。位置合わせは Edit > Grid and Snap Settings の Move スナップを **1u**(マス=4u の 1/4)にしておくと揃えやすい
3. **Ctrl+S** で保存。変更したシーンだけが保存される
4. コミットするのは**自分の `Field_Area01_Props_*.unity` だけ**。`Field_Area01.unity` に差分が出ていたら誤操作なので `git checkout` で戻す

## マップを直したくなったら(**マップ形状担当だけ**が行う)

**図の編集と再生成はマップ形状担当の権限**。小物配置担当は `documents/MapLayout.md` を編集せず、
`Rebuild Field_Area01` も実行しない。

- **理由**: `Field_Area01.unity` は図から作られる**生成物**なので、複数人が各自の環境で再生成してコミットすると、
  同じ内容でも差分がぶつかり、どれが正しい状態か分からなくなる。図の更新と再生成を1人に集約すれば、
  シーンの正は常に1つになる。

手順(マップ形状担当)は、図(`documents/MapLayout.md`)を直して
**`Tools > CreativeAI > Map > Rebuild Field_Area01`**。
作り直すのは **`Map` ルートだけ**なので、小物は消えない(別シーンにあるうえ、同じシーンでも `Map` の外なら残る)。
小物シーンを開いたまま実行しても、開いているシーンは閉じない / 生成先は必ずマップシーンになる、という作りにしてある。