# セーブシステム

ゲームの進行状態を **単一スロットの JSON** として保存・復元する仕組み。何を保存するか・いつ保存するかの **正本** はこのドキュメント(`GameSystems.md` 側は概要と参照のみ)。

- **形式** … JSON(理由は 5章)
- **スロット** … 単一スロット上書き。**マニュアルセーブのみ**(自動セーブは持たない → 3章)
- **思想** … セーブデータは「DB」ではなく **1個のスナップショット**。起動時に全読み、セーブ時に全書き

---

## 1. 何を保存するか(SaveData スキーマ)

セーブデータ = `SaveData` クラス1つにまとめてシリアライズする。

| 区分 | 項目 | 内容 |
| ---- | ---- | ---- |
| 進行 | `progress` | **メイン進行度(整数1つ)**(→ 1.1) |
| 進行 | `flags` | 分岐フラグ(key → 値の辞書)。`choice` の選択結果(→ 1.1) |
| プレイヤー | `currentScene` | 復帰するフィールドシーン名(セーブは移動中のみ → 3章) |
| プレイヤー | `position` / `rotation` | プレイヤーの座標・向き |
| プレイヤー | `currentHp` | 現在HP(最大HPは装備から再計算するので保存しない → 2章) |
| 所持 | `inventory` | 所持アイテムの一覧(個体単位 → 1.2) |
| 装備 | `equipped` | 装備中の装備品3スロット＋武器1スロット(個体への参照 → 1.2) |

> 進行度とインベントリは **独立した軸**。「アイテムを持っているか」はイベント発火条件にもなる(`StoryProgressionSystem.md` 条件タイプ)が、進行度に畳まずインベントリ側を読む。

### 1.1 進行度とフラグ

- `progress`: メイン進行度(整数1つ)。メインストーリーの進行に使う
- `flags`: 進行度に畳めない分岐を持つ key → 値の辞書(例: `{ "fox_choice": "together" }`)。会話中の `choice` で書き込まれ、`flag` 条件で参照される(`StoryProgressionSystem.md`)

### 1.2 アイテムの表現(個体 = ロール済みステータス込み)

調合で生成される装備品・食材は **同じレシピでも個体ごとにステータスが違う**(ディリクレでロール → `CraftingStatAlgorithm.md`)。マスタデータ(名前・画像・カテゴリ)は調合DB/SOが持ち、**ロール済みステータスはセーブ側で個体ごとに保存**する(`CraftingArchitecture.md`「所持品・ロール済みステータスは別管理」)。

```
inventory: [
  {
    instanceId : "uuid-1",        # セーブ内で一意。装備参照のキー
    itemId     : "amulet_atk",    # マスタへの参照 → 名前・画像・カテゴリを引く
    rolledStats: { 攻撃%: 12, 防御%: 4 },  # ロール済み。付与は最大2つ
    count      : 1
  }
]
```

- ロール済みステータスを持つ個体(装備品・食材)は一意。`count = 1` で1件ずつ持つ(個体差が消えてはいけない)
- ステータスを持たない素朴なアイテム(拾得素材など)は `rolledStats` を空にして `count` でまとめてよい
- マスタが持つ情報は保存せず、`itemId` から都度引く(重複・不整合を避ける)

```
equipped: {
  accessories: ["uuid-1", "uuid-7", null],  # 装備品スロット3つ。空きは null
  weapon     : "uuid-3"                      # 武器スロット1つ。null 可
}
```

- 装備は **個体への参照(instanceId)だけ** を持ち、実体は inventory 側にある(二重に持たない)
- ロード時にこの参照から装備を復元し、ステータス補正を反映する

---

## 2. 何を保存しないか(境界)

**再計算/再構築できるものは保存しない**(肥大化・不整合を避ける)。

| 保存しない | 理由 |
| ---------- | ---- |
| 最大HP | 装備の「最大HP%」から **ロード時に再計算**できる(保存すると装備状態と二重管理になり矛盾の元) |
| アイテムのマスタ情報(名前・画像・カテゴリ) | `itemId` から引ける |
| 敵の状態・戦闘の途中経過 | セーブは移動中のみ(戦闘モード中は不可)。戦闘は勝敗を記録しない(`StoryProgressionSystem.md`) |

---

## 3. いつ保存するか(発動条件)

**セーブはマニュアルセーブのみ**。自動セーブ(チェックポイント/章節目オート等)は持たない。

| 種別 | 発動 | 制約 |
| ---- | ---- | ---- |
| マニュアルセーブ | プレイヤーが **セーブUI** から任意に実行 | **移動中のみ**(戦闘モード中・会話UI表示中は不可 → モードの正本は [GameMode.md](./GameMode.md)) |

- スロットは単一。マニュアルセーブのたびに同じスロットへ上書きされる
- 死亡時は単一スロットの最新状態から再開(= 直近のマニュアルセーブが起点)

> 移動中(戦闘モードでないとき)しかセーブできないため、戦闘・会話途中のスナップショットは存在しない。

---

## 4. 保存先・スロット

| 項目 | 内容 |
| ---- | ---- |
| 保存先 | `Application.persistentDataPath`(プラットフォーム差を吸収する標準パス) |
| ファイル | 単一ファイル(例: `save.json`) |
| 書き込み | **アトミック書き込み**(一時ファイルに書き切ってから本ファイルへ rename)。書き込み途中の電源断でファイルが壊れない |

---

## 5. 形式: JSON

**JSON を採用**:

- セーブは **単一スロットの小さなスナップショット**。部分的なクエリが要らず、起動時に全読み・セーブ時に全書きで足りる
- **テキストなので開いて中身を確認・書き換えできる**(進行度を直接いじってデバッグ、という `StoryProgressionSystem.md` の狙いと噛み合う)
- Unity 標準機能だけで完結。買い切り・ローカル方針(サーバー不要)に合う

ライブラリは **Newtonsoft(`com.unity.nuget.newtonsoft-json`)**。`JsonUtility` は `Dictionary`・null・多態に弱く、本スキーマは `rolledStats`(辞書)や `equipped` の null スロットを持つため。

---

## 6. ロード時の復元順序

依存関係があるので順序を固定する。

```
1. ファイル読込
2. inventory を復元(個体＋ロール済みステータス)
3. equipped の参照から装備を復元 → 最大HP等のステータス補正を反映
4. currentScene をロード(移動シーン)
5. position / rotation を反映
6. progress / flags を ProgressManager に設定
7. currentHp を反映(最大HP確定後なのでクランプできる)
```

> 装備 → 最大HP確定 → 現在HP反映 の順にしないと現在HPを最大HPでクランプできない。これがインベントリ→装備→HP の依存理由。
